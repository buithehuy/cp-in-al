# Training configuration
epochs_per_round: 10
batch_size: 128
lr: 0.01
momentum: 0.9
weight_decay: 5e-4
optimizer: sgd
scheduler: cosine
device: cuda
use_amp: true  # Automatic Mixed Precision (2-3x speedup on GPU)
