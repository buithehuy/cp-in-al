# Training configuration
init_epochs: 10
epochs_per_round: 10
lr: 0.01
momentum: 0.9
weight_decay: 5e-4
optimizer: sgd
scheduler: cosine
device: cuda
use_amp: true  # Automatic Mixed Precision (2-3x speedup on GPU)
